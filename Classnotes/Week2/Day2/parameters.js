

let x = 5;
let y = 6;

function add2(num1, num2) {
    let total = num1 + num2;
    console.log(`${num1} + ${num2} = ${total}`);
}

add2(x, y)
add2(7, 10);

// num1 an num2 are parameters and its a way to get info into the function.

